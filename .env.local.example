# Correos autorizados para entrar a la calculadora (separados por coma)
ALLOWED_EMAILS=ayruorganic@gmail.com,otra.persona@correo.com

# Secreto para firmar la sesión (mínimo 32 caracteres). Genera uno con:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
